import { ValidationError, Validator, ValidatorAdapterParams } from '@tanstack/form-core';
import { AnySchema, ValidationError as YupError } from 'yup';
type Params = ValidatorAdapterParams<YupError>;
type TransformFn = NonNullable<Params['transformErrors']>;
export declare function prefixSchemaToErrors(yupErrors: YupError[], transformErrors: TransformFn): Record<string, ValidationError>;
export declare function defaultFormTransformer(transformErrors: TransformFn): (zodErrors: YupError[]) => {
    form: ValidationError;
    fields: Record<string, ValidationError>;
};
export declare const yupValidator: (params?: Params) => Validator<unknown, AnySchema>;
export {};
